import React from "react";
import Home from "./Home";

// Transport Overview page - uses the same content as Home for now
const TransportOverview = () => {
  return <Home />;
};

export default TransportOverview;