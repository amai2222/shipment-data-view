import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ContractFileManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>文件管理</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">合同文件管理功能正在开发中...</p>
      </CardContent>
    </Card>
  );
}