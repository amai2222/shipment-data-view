import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ContractTagAssignment() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>标签分配</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">合同标签分配功能正在开发中...</p>
      </CardContent>
    </Card>
  );
}