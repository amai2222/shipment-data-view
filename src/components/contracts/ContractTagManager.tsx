import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ContractTagManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>标签管理</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">合同标签管理功能正在开发中...</p>
      </CardContent>
    </Card>
  );
}