import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ContractNumberingManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>编号管理</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">合同编号管理功能正在开发中...</p>
      </CardContent>
    </Card>
  );
}