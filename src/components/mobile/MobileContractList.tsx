import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function MobileContractList() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>合同列表</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">移动端合同列表功能正在开发中...</p>
      </CardContent>
    </Card>
  );
}