# Cloudflare Pages 重定向规则
# 只重定向不存在的路由到 index.html，不影响静态资源

# 排除静态资源路径，确保 JS/CSS/图片等文件直接返回
/assets/*    200
/*.js        200
/*.css       200
/*.png       200
/*.jpg       200
/*.jpeg      200
/*.gif       200
/*.svg       200
/*.ico       200
/*.woff      200
/*.woff2     200
/*.ttf       200
/*.eot       200
/*.json      200

# 其他所有路由重定向到 index.html（SPA 路由支持）
/*    /index.html   200
