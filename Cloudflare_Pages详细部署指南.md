# Cloudflare Pages 详细部署指南

## 📊 三个关键问题的答案

### ❓ 部署方便吗？
**✅ 非常方便！** 和 Vercel 一样简单

### ❓ 免费吗？
**✅ 完全免费！** 而且是**无限带宽**！

### ❓ 有即时预览吗？
**✅ 有！** 每个分支/PR 都有预览链接

---

## 🎁 Cloudflare Pages 免费额度

### 免费计划包括：

```
✅ 无限带宽！（重点！）
✅ 无限请求
✅ 500 次构建/月
✅ 1 次并发构建
✅ 无限静态网站
✅ 自动 HTTPS
✅ 全球 CDN（300+ 节点）
✅ 分支预览部署
✅ Git 集成
✅ 自定义域名
✅ Web Analytics
```

**对比**：
- Vercel：100GB 带宽/月
- Netlify：100GB 带宽/月
- **Cloudflare：无限带宽！** 🎉

---

## 🚀 部署步骤（超简单）

### 方式 1：通过网页部署（5分钟）

#### 步骤 1：注册 Cloudflare
1. 访问 [pages.cloudflare.com](https://pages.cloudflare.com)
2. 使用邮箱注册（或 GitHub 登录）
3. 完全免费，无需信用卡

#### 步骤 2：连接 Git 仓库
1. 点击 "Create a project"
2. 选择 "Connect to Git"
3. 授权 GitHub/GitLab/Bitbucket
4. 选择你的仓库

#### 步骤 3：配置构建

**自动检测**（推荐）：
- Cloudflare 会自动识别 Vite 项目
- 自动填充构建命令

**手动配置**：
```
项目名称: shipment-data-view
生产分支: main

构建设置:
- Framework preset: None（或 Vite）
- Build command: npm run build
- Build output directory: dist
- Root directory: /（默认）
```

#### 步骤 4：添加环境变量
点击 "Environment variables"：
```
VITE_SUPABASE_URL = https://你的项目.supabase.co
VITE_SUPABASE_ANON_KEY = 你的匿名密钥
VITE_ENABLE_PWA = false
```

**重要**：选择 "Production" 和 "Preview" 都添加

#### 步骤 5：部署
1. 点击 "Save and Deploy"
2. 等待 2-3 分钟
3. 完成！获得 `https://your-project.pages.dev` 地址

---

### 方式 2：通过 CLI 部署

#### 安装 Wrangler CLI
```bash
npm install -g wrangler

# 或使用 npx（无需安装）
npx wrangler
```

#### 登录
```bash
wrangler login
```

#### 部署
```bash
# 1. 构建项目
npm run build

# 2. 部署
npx wrangler pages deploy dist --project-name=shipment-data-view

# 首次部署会提示创建项目
# 选择项目名称
# 完成！
```

#### 设置环境变量
```bash
# 添加生产环境变量
npx wrangler pages secret put VITE_SUPABASE_URL
npx wrangler pages secret put VITE_SUPABASE_ANON_KEY
```

---

## 🔍 即时预览功能（Preview Deployments）

### 功能说明

**每个分支自动部署**：
```
main 分支 → https://shipment-data-view.pages.dev
dev 分支  → https://dev.shipment-data-view.pages.dev
feature/x → https://feature-x.shipment-data-view.pages.dev
```

**每个 Pull Request**：
```
PR #1 → https://pr-1.shipment-data-view.pages.dev
PR #2 → https://pr-2.shipment-data-view.pages.dev
```

### 使用场景

**1. 开发分支预览**
```bash
# 创建新分支
git checkout -b feature/new-feature

# 提交代码
git add .
git commit -m "添加新功能"
git push origin feature/new-feature

# ✅ Cloudflare 自动构建
# ✅ 获得预览链接
# ✅ 可以立即查看效果
```

**2. PR 审查**
```
开发者提交 PR
    ↓
Cloudflare 自动构建预览
    ↓
审查者点击预览链接查看
    ↓
测试功能
    ↓
批准合并
```

**3. 客户演示**
```
# 为客户创建演示分支
git checkout -b demo/client-preview

# 定制内容
# ...

# 推送
git push origin demo/client-preview

# 分享预览链接给客户
# https://demo-client-preview.your-project.pages.dev
```

---

## 📋 详细配置

### 在项目根目录创建配置文件

#### 创建 `wrangler.toml`（可选）
```toml
name = "shipment-data-view"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

[env.production]
name = "shipment-data-view"
route = "https://your-domain.com/*"

[env.preview]
name = "shipment-data-view-preview"
```

### 构建配置优化

在 Cloudflare Pages 项目设置中：

**Build configurations**：
```
Build command: npm run build
Build output directory: dist
Root directory: /
Node version: 18
```

**环境变量**（Production & Preview）：
```
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key
VITE_ENABLE_PWA=false
NODE_VERSION=18
NPM_VERSION=9
```

---

## 🌐 自定义域名

### 添加域名

#### 步骤 1：在 Cloudflare Pages 中添加
1. 进入项目设置
2. Custom domains
3. 点击 "Set up a custom domain"
4. 输入域名（如 www.your-domain.com）

#### 步骤 2：配置 DNS

**如果域名在 Cloudflare**：
- ✅ 自动配置
- ✅ 无需手动操作

**如果域名在其他注册商**：
添加 CNAME 记录：
```
类型: CNAME
名称: www
值: your-project.pages.dev
TTL: 自动或 3600
```

#### 步骤 3：等待生效
- DNS 生效：几分钟到几小时
- 自动配置 HTTPS
- 自动续期 SSL 证书

---

## 🔄 自动部署流程

### Git 推送触发部署

```
开发者推送代码
    ↓
GitHub/GitLab 触发 Webhook
    ↓
Cloudflare Pages 接收通知
    ↓
开始构建（npm install）
    ↓
运行构建命令（npm run build）
    ↓
部署到全球 CDN
    ↓
发送通知（可选）
    ↓
完成！
```

### 查看部署状态

**在 Cloudflare Dashboard**：
- 实时构建日志
- 部署历史
- 回滚功能
- 性能分析

**在 Git 仓库**：
- Commit 旁显示部署状态
- PR 中显示预览链接
- 失败时显示错误信息

---

## 🎯 与 Vercel/Netlify 对比

| 特性 | Cloudflare | Vercel | Netlify |
|------|-----------|--------|---------|
| **免费带宽** | ✅ 无限 | ❌ 100GB | ❌ 100GB |
| **构建次数** | ✅ 500次/月 | ✅ 无限 | ✅ 300分钟 |
| **部署速度** | ⚡ 快 | ⚡ 快 | ⚡ 快 |
| **预览部署** | ✅ 有 | ✅ 有 | ✅ 有 |
| **自定义域名** | ✅ 免费 | ✅ 免费 | ✅ 免费 |
| **HTTPS** | ✅ 自动 | ✅ 自动 | ✅ 自动 |
| **全球 CDN** | ✅ 300+节点 | ✅ 全球 | ✅ 全球 |
| **国内访问** | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ |
| **易用性** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Web Analytics** | ✅ 免费 | ❌ 付费 | ❌ 付费 |

---

## 💡 Cloudflare Pages 的优势

### 1. 无限带宽 🎉
```
其他平台：流量超限需要付费
Cloudflare：永远不用担心流量！

适合：
- 高流量网站
- 突发流量场景
- 不想担心成本
```

### 2. 全球最快的 CDN 🌍
```
Cloudflare CDN：
- 300+ 个节点
- 覆盖 100+ 个国家
- 毫秒级响应
- 自动智能路由
```

### 3. 内置安全功能 🔒
```
免费包含：
- DDoS 防护
- WAF（Web 应用防火墙）
- SSL/TLS 加密
- Bot 保护
```

### 4. Workers 集成 ⚡
```
可以添加边缘计算：
- API 路由
- 图片优化
- A/B 测试
- 地理位置路由
```

---

## 🛠️ 高级功能

### 1. 重定向规则

在 `_redirects` 文件（dist 目录）：
```
# SPA 路由
/* /index.html 200

# 重定向旧 URL
/old-page /new-page 301

# 自定义 404
/404 /index.html 404
```

### 2. Headers 配置

在 `_headers` 文件（dist 目录）：
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin

/assets/*
  Cache-Control: public, max-age=31536000, immutable
```

### 3. Functions（边缘函数）

创建 `functions` 目录：
```javascript
// functions/api/hello.js
export async function onRequest(context) {
  return new Response('Hello from Edge!');
}
```

访问：`https://your-site.pages.dev/api/hello`

---

## 📊 监控和分析

### Web Analytics（免费）

**在 Cloudflare Dashboard 启用**：
1. 进入 Web Analytics
2. 点击 "Add a site"
3. 选择你的 Pages 项目
4. 自动集成

**数据包括**：
- 页面访问量
- 独立访客
- 跳出率
- 页面加载时间
- 国家/地区分布
- 浏览器分布
- 设备分布

**隐私友好**：
- 不使用 Cookie
- 不追踪个人信息
- 符合 GDPR

---

## 🔍 故障排查

### 构建失败

**检查构建日志**：
1. 进入 Cloudflare Dashboard
2. 选择项目
3. 查看 "Deployments"
4. 点击失败的部署
5. 查看详细日志

**常见问题**：

```bash
# 问题 1：node 版本不匹配
# 解决：设置环境变量 NODE_VERSION=18

# 问题 2：依赖安装失败
# 解决：检查 package.json，确保依赖正确

# 问题 3：构建命令错误
# 解决：确认 Build command 是 npm run build

# 问题 4：输出目录错误
# 解决：确认 Build output directory 是 dist
```

### 页面空白

**检查**：
1. 浏览器控制台错误
2. 环境变量是否设置
3. API 是否可访问
4. 路由配置是否正确

**解决**：
```
# 添加 _redirects 文件
/* /index.html 200
```

---

## 🎯 最佳实践

### 1. 分支策略
```
main → 生产环境
develop → 开发环境（预览）
feature/* → 功能预览
hotfix/* → 紧急修复预览
```

### 2. 环境变量管理
```
Production：生产环境变量
Preview：预览环境变量（可以用测试数据）
```

### 3. 构建优化
```javascript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          // 代码分割
        }
      }
    }
  }
});
```

---

## 💰 成本计算

### 免费额度足够吗？

**示例计算**：
```
假设你的网站：
- 每月 10 万访客
- 每次访问加载 2MB
- 总流量：200GB

Vercel：免费额度 100GB → ❌ 超限，需付费
Netlify：免费额度 100GB → ❌ 超限，需付费
Cloudflare：无限带宽 → ✅ 完全免费！

构建次数：
- 每天部署 5 次
- 每月 150 次
- Cloudflare 限制：500 次/月 → ✅ 完全够用
```

### 付费升级（如需要）

```
Cloudflare Pages Pro（$20/月）：
- 5,000 次构建/月
- 5 个并发构建
- 更多 Functions 调用
- 优先支持

适合：
- 大型团队
- 高频部署
- 企业项目
```

---

## 📝 快速上手清单

### ✅ 部署前
- [ ] 代码已推送到 Git
- [ ] 确认构建命令：`npm run build`
- [ ] 确认输出目录：`dist`
- [ ] 准备好 Supabase 环境变量

### ✅ 部署中
- [ ] 注册 Cloudflare Pages
- [ ] 连接 Git 仓库
- [ ] 配置构建设置
- [ ] 添加环境变量
- [ ] 点击部署

### ✅ 部署后
- [ ] 测试网站功能
- [ ] 检查环境变量
- [ ] 配置自定义域名（可选）
- [ ] 启用 Web Analytics（可选）
- [ ] 设置通知（可选）

---

## 🎉 总结

### Cloudflare Pages 的答案

| 问题 | 答案 |
|------|------|
| **方便吗？** | ✅ 非常方便！和 Vercel 一样简单 |
| **免费吗？** | ✅ 完全免费！**无限带宽**！ |
| **有预览吗？** | ✅ 有！每个分支/PR 都有预览链接 |

### 推荐指数：⭐⭐⭐⭐

**适合场景**：
- ✅ 高流量网站（无限带宽）
- ✅ 国际用户为主（全球 CDN）
- ✅ 不想担心流量成本
- ✅ 需要预览部署功能
- ✅ 对国内访问速度要求不极致（2-5秒可接受）

**不太适合**：
- ❌ 纯国内用户（建议用腾讯云 Webify）
- ❌ 需要 <1秒 加载速度（建议用国内 CDN）

---

## 🚀 立即部署

### 快速命令

```bash
# 方法 1：网页部署（推荐新手）
# 访问 pages.cloudflare.com
# 点击 Create a project → 跟随指引

# 方法 2：CLI 部署（推荐开发者）
npm run build
npx wrangler pages deploy dist --project-name=shipment-data-view
```

**5 分钟后，你的网站就上线了！** 🎊

---

需要我帮你配置具体的部署步骤吗？😊

