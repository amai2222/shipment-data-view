# 日期自动同步说明

## 📅 功能概述

为了让 AI 助手始终使用您当前的系统日期（而不是 AI 训练数据中的日期），我们配置了**自动日期同步功能**。

## 🎯 工作原理

### 1. 日期配置文件

**文件位置**：项目根目录 `.cursor-date`

**文件内容**：
```
# Cursor AI 当前日期配置
# 此文件用于同步 AI 助手使用的当前日期
# 此文件由 scripts/update-cursor-date.js 自动更新
# 最后更新：2025-11-16 14:30:00

CURRENT_DATE=2025-11-16
```

### 2. 自动更新脚本

我们提供了三种自动更新脚本：

- **Node.js**：`scripts/update-cursor-date.js`（推荐，跨平台）
- **Shell**：`scripts/update-cursor-date.sh`（Linux/Mac）
- **PowerShell**：`scripts/update-cursor-date.ps1`（Windows）

## 🚀 使用方法

### 方法1：使用 npm 脚本（推荐）

在项目根目录运行：

```bash
npm run update-date
```

这会自动读取系统当前日期并更新 `.cursor-date` 文件。

### 方法2：使用 Cursor 任务（自动运行）

已配置 Cursor 任务，在打开项目时自动更新日期：

1. 打开 Cursor
2. 按 `Ctrl+Shift+P`（Windows/Linux）或 `Cmd+Shift+P`（Mac）
3. 输入 `Tasks: Run Task`
4. 选择 `更新 Cursor 日期`

**自动运行**：任务已配置为在打开文件夹时自动运行（`runOn: "folderOpen"`）。

### 方法3：直接运行脚本

#### Windows（PowerShell）
```powershell
powershell -ExecutionPolicy Bypass -File scripts/update-cursor-date.ps1
```

#### Linux/Mac（Shell）
```bash
chmod +x scripts/update-cursor-date.sh
./scripts/update-cursor-date.sh
```

#### 跨平台（Node.js）
```bash
node scripts/update-cursor-date.js
```

## ⚙️ 自动配置

### Cursor 任务配置

已创建 `.vscode/tasks.json` 文件，配置了自动更新任务：

```json
{
  "label": "更新 Cursor 日期",
  "runOptions": {
    "runOn": "folderOpen"
  }
}
```

**注意**：如果自动运行不工作，可以手动运行任务。

### 添加到启动脚本

您也可以将更新脚本添加到系统启动脚本中，或者创建一个快捷方式。

## ✅ 验证配置

### 检查日期是否更新

1. 运行更新脚本：`npm run update-date`
2. 打开 `.cursor-date` 文件
3. 确认 `CURRENT_DATE` 为当前日期

### 测试 AI 助手

1. 更新日期后，询问 AI 助手："今天是什么日期？"
2. AI 助手应该回答 `.cursor-date` 文件中的日期

## 🔄 自动更新流程

### 推荐工作流

1. **打开 Cursor** → 任务自动运行（如果配置了 `runOn: "folderOpen"`）
2. **或者手动运行** → `npm run update-date`
3. **AI 助手自动读取** → `.cursor-date` 文件中的日期

### 每日工作流

建议每天打开项目时运行一次：

```bash
npm run update-date
```

或者创建一个别名：

```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
alias update-cursor-date="cd /path/to/project && npm run update-date"
```

## 📋 日期格式

### 标准格式
- **配置文件**：`YYYY-MM-DD`（如：`2025-11-16`）
- **文档**：`YYYY-MM-DD`（如：`2025-11-16`）
- **代码注释**：`YYYY-MM-DD`（如：`2025-11-16`）

### 文件名格式
- **SQL 迁移文件**：`YYYYMMDD_`（如：`20251116_add_feature.sql`）

## 🔧 故障排除

### 问题1：脚本无法运行

**解决方案**：
- 确保已安装 Node.js（用于 Node.js 脚本）
- 检查脚本文件权限（Linux/Mac）
- 检查 PowerShell 执行策略（Windows）

### 问题2：任务不自动运行

**解决方案**：
- 手动运行任务：`Ctrl+Shift+P` → `Tasks: Run Task` → `更新 Cursor 日期`
- 或者直接运行：`npm run update-date`

### 问题3：日期格式错误

**解决方案**：
- 检查系统日期格式设置
- 手动编辑 `.cursor-date` 文件
- 确保格式为 `YYYY-MM-DD`

## 💡 提示

- **自动化**：可以设置系统定时任务每天自动运行脚本
- **快捷方式**：创建桌面快捷方式快速更新日期
- **验证**：如果不确定日期是否正确，可以询问 AI 助手

## 📝 脚本说明

### update-cursor-date.js（Node.js）

跨平台脚本，推荐使用。

```bash
node scripts/update-cursor-date.js
```

### update-cursor-date.sh（Shell）

Linux/Mac 使用。

```bash
chmod +x scripts/update-cursor-date.sh
./scripts/update-cursor-date.sh
```

### update-cursor-date.ps1（PowerShell）

Windows 使用。

```powershell
powershell -ExecutionPolicy Bypass -File scripts/update-cursor-date.ps1
```

---

**文档版本**：v2.0  
**创建日期**：2025-11-16  
**最后更新**：2025-11-16
